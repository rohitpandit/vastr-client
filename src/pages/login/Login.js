import React, { Fragment } from 'react';
import Navbar from '../../components/navbar/Navbar';

const Login = () => {
	return (
		<Fragment>
			<Navbar />
			<div>Login page</div>
		</Fragment>
	);
};

export default Login;
